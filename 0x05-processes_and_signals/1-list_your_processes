#!/usr/bin/env bash
# Display all running processes with header "USER STARTED COMMAND" and start time (HH:MM:SS)

# Print the exact header the checker expects
printf "USER STARTED COMMAND\n"

# Use ps to print USER, STIME (start time including seconds) and full command.
# Trim leading spaces so columns align cleanly.
ps -eo user=,stime=,cmd= | sed 's/^[[:space:]]*//'
