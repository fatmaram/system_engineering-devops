#!/usr/bin/env bash
# This script kills the 7-highlander process using SIGKILL (signal 9)

# Find all PIDs of the 7-highlander process
PIDS=$(pgrep -f 7-highlander)

# Kill each PID safely
if [ -n "$PIDS" ]; then
  kill -9 $PIDS
  echo "OK"
fi
